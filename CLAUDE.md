# CLAUDE.md — ProjectLens 開発ガイド (v1.1)

このファイルは Claude Code がこのリポジトリで開発を行う際に**必ず遵守する**ルールと参照情報を定義する。

---

## 1. プロジェクト概要

- **プロダクト名**: ProjectLens
- **概要**: ソースコードプロジェクトを解析し、アーキテクチャドキュメントを自動生成するデスクトップアプリ。静的解析（tree-sitter）+ AI解析（社内AI **NewtonX**）で構造・依存関係・技術的負債を可視化する
- **解析対象**: ソースコード + **RPA定義ファイル（Power Platform / Power Automate for Desktop / UiPath）**（04 §8）。コンパイル済みバイナリは対象外
- **対象OS**: **Windowsのみ**
- **出力言語**: 生成ドキュメント・UIとも**日本語のみ**（i18n基盤は導入する）

### 確定技術スタック（変更禁止。変更が必要な場合は必ずユーザーに確認）

| レイヤ | 技術 |
|---|---|
| デスクトップ基盤 | Tauri v2 |
| バックエンド | Rust（tokio / serde / thiserror / rusqlite） |
| フロントエンド | React + TypeScript（Vite） |
| 状態管理 / i18n | Zustand / react-i18next |
| UI | Tailwind CSS + shadcn/ui |
| 静的解析 | tree-sitter |
| AI | NewtonX（Python ADK を**サイドカー**経由で利用） |
| DB | SQLite |

### 必読ドキュメント（実装前に必ず読むこと）

| ファイル | 内容 |
|---|---|
| `docs/01_要件定義書.md` | 背景・目的、機能要件、非機能要件 |
| `docs/02_仕様書.md` | 4フェーズ処理フロー、キャッシュ仕様、Tauriイベント、リトライ戦略 |
| `docs/03_設計書.md` | レイヤー構成、ER図/DDL、**§8.1 推奨実装順序（この順で実装）** |
| `docs/04_実装詳細.md` | **確定事項（最優先）**: NewtonX連携設計、configデフォルト値、AI応答JSONスキーマ、プロンプト全文、コマンドシグネチャ |

**優先順位**: 04 > 03 > 02 > 01（矛盾時は番号の大きい方を優先し、乖離を見つけたら報告する）

---

## 2. 最重要原則（違反コードは書かない）

### 原則1: ゼロハードコーディング

**目的**: 横展開漏れの防止。値の変更が「1ファイルの1箇所の修正」で完結する状態を維持する。

#### 2.1.1 画面ラベル・メッセージは全て外部ファイルに定義する

- UIに表示する文字列（ラベル、ボタン名、エラーメッセージ、トースト文言等）を **コンポーネント内に直書きすることを禁止** する
- 全て `src/locales/ja.json` に定義し、react-i18next のキー経由で参照する（言語は日本語のみだが、この構造自体がゼロハードコーディングの実現手段である）

```tsx
// ❌ 禁止
<button>キャンセル</button>

// ✅ 必須
<button>{t("analysis.cancel")}</button>
```

#### 2.1.2 動作パラメータは全て config.json に外出しする

- 数値・パス・上限値・タイムアウト等のパラメータをコード内に直書きしない
- `docs/04_実装詳細.md` §4 のデフォルト値定義に従い、**設定画面（アプリUI）から変更可能にする**
- デフォルト値はRust側の `Default` 実装に**一箇所だけ**定義し、リセット機能もこれを参照する

```rust
// ❌ 禁止
let timeout = 90; // 秒

// ✅ 必須
let timeout = config.ai.timeout_secs;
```

- **並列数（concurrency）は特に重要**: NewtonXのレート制限が未確定のため、ユーザーがUIから即座に変更できること

#### 2.1.3 定数・列挙値は定義ファイルに集約する

- イベント名（`analysis://progress` 等）、フェーズ名、エラーカテゴリ、SQLテーブル名、既定出力パス等の文字列リテラルは、**Rust側は `models/constants.rs` / enum、フロント側は `src/lib/constants.ts`** に一元定義する
- 文字列のコピペ多重定義を禁止する（typoによる横展開漏れの温床になるため）
- **プロンプト本文と `PROMPT_VERSION` は `domain/ai_analyzer/prompts.rs` に集約**する。プロンプトを変更したら必ずバージョンを更新する（キャッシュキーと連動）
- NewtonXサイドカーのRPCメソッド名も Rust / Python 双方から参照される定数として一元管理する

### 原則2: DRY（同じ処理は一箇所に）

**目的**: 保守工数削減と横展開漏れ防止。同一ロジックの修正が1箇所で完結する状態を維持する。

#### 2.2.1 共通化必須の処理

| 処理 | 集約先 | 備考 |
|---|---|---|
| NewtonXリトライ（認証切れ/レート制限/通信エラー） | `infra/ai_client/retry.rs` | ファイル解析・全体解析・接続テストで共用 |
| AI応答バリデーション（前処理→パース→検証→補完） | `domain/ai_analyzer/validator.rs` | ファイル/プロジェクトのスキーマ差はジェネリクスで吸収 |
| キャッシュキー生成・TTL判定 | `infra/cache_repo.rs` | file/project 両キャッシュで共用 |
| SHA-256ハッシュ生成 | `domain/scanner/hash.rs` | ファイル/プロジェクト合成ハッシュで共用 |
| エラー→通知方法のマッピング | `models/error.rs` + フロント1箇所 | 設計書§7.2の対応表を単一実装にする |
| 進捗イベント発火 | `domain/orchestrator.rs` のヘルパー | 各フェーズから直接 emit しない |
| Tauri invoke ラッパー・イベント購読 | `src/lib/tauri.ts` | 04 §7 のシグネチャに準拠した型付きラッパー |
| サイドカーRPC送受信 | `infra/ai_client/bridge.rs`（Rust）/ `newtonx_bridge.py` の dispatcher（Python） | RPC処理をメソッドごとに複製しない |
| Mermaidレンダリング・セクション組み立て | `domain/doc_generator/` | MD/HTML/JSON はレンダラのみ差し替え |

#### 2.2.2 実装ルール

- 同じ処理を2箇所目に書きそうになったら、**書く前に共通関数へ抽出**する（3回目を待たない）
- 言語別の静的解析は共通trait `LanguageAnalyzer` を定義し、tree-sitter文法の追加のみで言語拡張できる構造にする
- RPA解析は共通trait `RpaAnalyzer` を定義し、ツール追加（UiPath等）が実装追加のみで済む構造にする。RPA用プロンプトは `RPA_PROMPT_VERSION` として独立バージョン管理する（04 §8.4）
- AIプロバイダは `AiClient` trait で抽象化を維持する（現状の実装は NewtonX のみだが、呼び出し側はtraitのみに依存すること）
- MD / HTML / JSON エクスポートは「解析結果 → 中間表現 → 各レンダラ」構造とし、組み立てロジックを形式ごとに複製しない

---

## 3. NewtonX 連携の遵守事項

- Rustから NewtonX の REST を直接叩かない。**必ず Python サイドカー（ADK利用）経由**とする（04 §3.2）
- `send_message` は**必ず `web_search=False` と `knowledge_search=False` を明示指定**する（既定値のまま送信することを禁止）
- 解析用チャットは `create_chat_in_folder_by_name` で**専用フォルダ「ProjectLens」配下に作成**する
- 解析セッションのチャットは終了時（完了・キャンセル・エラーとも）に必ず削除する（`session.close`）
- 認証はADKのトークン管理（`authenticate_auto`）に委譲する。トークン・認証情報をログ出力しない
- レート制限・入力上限は未確定（04 §9）。実装時に実測し、判明した値は config デフォルトと docs に反映する

---

## 4. 実装の進め方

1. `docs/03_設計書.md` §8.1 の実装順序に従う。ただし **Step 6（AiClient）は「Pythonサイドカー + ブリッジ」の実装に読み替える**
2. 設定管理（config.json）・constants・locales の基盤を**最初に**作る。以降の全実装はこれを参照する
3. 各ステップ完了時に設計書 §8.2 のDoDでセルフチェックする
4. 04 §9 の残課題に該当する事項（レート制限実値等）が判明したら、コードだけでなく docs も更新する
5. ドキュメント未定義の判断が必要になった場合は、**独断で確定せず選択肢を提示してユーザーに確認**する

---

## 5. コーディング規約（要点）

- **Rust**: `thiserror` エラー型（設計書§7.1）、tokio非同期、並列数はセマフォ制御（config値）、キャンセルは `CancellationToken` を全フェーズ+サイドカー呼び出しに伝播
- **フロントエンド**: 状態はZustandストアに集約。イベント購読はマウント時listen/アンマウント時解除。文字列は必ず `t()` 経由
- **Python（サイドカー）**: 標準入出力JSON Lines RPCのみ。標準出力にRPC応答以外を出力しない（ログはstderr）
- **SQLite**: DDLは設計書§3.2。マイグレーションは `db_metadata.schema_version` で管理
- **テスト**: リトライ分岐（認証切れ/レート制限/通信エラー/応答不正/フォールバック）とキャッシュ判定（ヒット/ミス/TTL切れ/プロンプトバージョン変更）は必ず単体テストを書く。サイドカーはモックブリッジで代替可能にする

---

## 6. レビュー時セルフチェックリスト

実装をコミットする前に、以下を必ず確認すること。

- [ ] UI文字列の直書きがない（全て locales 経由か）
- [ ] 数値・パス・上限値の直書きがない（全て config 経由か）
- [ ] イベント名・RPCメソッド名等の文字列リテラルが constants に集約されているか
- [ ] 同一・類似ロジックのコピペがない（共通関数へ抽出したか）
- [ ] 新しい設定項目を追加した場合、設定画面（UI）からも変更できるか
- [ ] デフォルト値の定義が1箇所か（リセット機能と一致しているか）
- [ ] プロンプトを変更した場合、`PROMPT_VERSION` を更新したか
- [ ] `send_message` に `web_search=False` / `knowledge_search=False` が明示指定されているか
- [ ] 解析用チャットの削除（cleanup）が全終了パスで実行されるか
- [ ] docs（01〜04）との乖離がないか。乖離があればdocsを更新したか
